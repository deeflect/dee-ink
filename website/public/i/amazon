#!/bin/sh
set -eu

if ! command -v cargo >/dev/null 2>&1; then
  echo "cargo is required. Install Rust from https://rustup.rs/" >&2
  exit 1
fi

crate="dee-amazon"
echo "Installing dee-amazon..."
cargo install "$crate"
